game:
  get:
    values:
      id:
        method: getId
      label:
        method: getLabel
      up:
        method: isUp
      map:
        method: getMap
      players:
        method: getPlayerCount

game-cmd:
  put:
    values:
      cmd:
        type: string
        required: true
